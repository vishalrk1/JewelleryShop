import { FormSchemaType } from "@/schemas";
import React from "react";

interface AddresFormProps {
  schema: FormSchemaType[];
  form: any;
}

const AddressForm: React.FC<AddresFormProps> = ({ schema, form }) => {
  return <></>;
};

export default AddressForm;
